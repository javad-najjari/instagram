from django.urls import path
from . import views



app_name = 'direct'
urlpatterns = [
    # path('all-directs/', views.AllDirectView.as_view()),
    # path('direct-detail/<str:username>/', views.DirectDetailView.as_view()),
    # path('like-direct-message/<int:message_id>/', views.LikeDirectMessageView.as_view()),
    # path('remove-direct-message/<int:message_id>/', views.RemoveDirectMessageView.as_view()),
    # path('search/<str:search>/', views.DirectSearchView.as_view()),
    # path('create-message/<int:direct_id>/', views.CreateMessageView.as_view()),
]
